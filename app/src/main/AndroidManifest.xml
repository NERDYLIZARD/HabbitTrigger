<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
  package="com.example.hoppies.habbittrigger">

  <!-- <uses-permission android:name="android.permission.WAKE_LOCK" /> -->
  <application
    android:allowBackup="true"
    android:icon="@mipmap/ic_launcher"
    android:label="@string/app_name"
    android:roundIcon="@mipmap/ic_launcher_round"
    android:supportsRtl="true"
    android:theme="@style/AppTheme">
    <!--<activity android:name=".TriggerListActivity">-->
    <activity android:name=".TriggerDetailActivity">
      <intent-filter>
        <action android:name="android.intent.action.MAIN" />

        <category android:name="android.intent.category.LAUNCHER" />
      </intent-filter>
    </activity>

    <receiver android:name=".OnTriggerReceiver" />

    <service android:name=".TriggerService" />

    <!--<activity-->
      <!--android:name=".TriggerDetailActivity"-->
      <!--android:parentActivityName=".TriggerListActivity"></activity>-->

  </application>

</manifest>